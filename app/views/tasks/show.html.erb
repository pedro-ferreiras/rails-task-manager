<h1><%= @task.title %></h1>


<p><%= @task.details %></p>

<%= link_to "Go back to tasks", tasks_path%>

<%= link_to "edit_task", edit_task_path(@task)%>

<%=link_to "Delete",
  delete_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
